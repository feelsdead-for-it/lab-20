#include <stdio.h> 
#include <ctype.h> 
#include <string.h> 
#include <iostream>
using namespace std;
int main()
{
	char c;
	char s[100];
	cout << "\n";
	gets_s(s);
	for (int i = 0; i < strlen(s); i++)
	{
		if (s[i+1] != ' ')
		{
			cout << s[i] << " ";
		}
		else
		{
			cout << s[i];
		}
	}
	
}
