#include <iostream>
#include <Math.h>
#include <windows.h>
#include <string>
using namespace std;
int main(void)
{
	int k = 0;
	string s, s0 ;
	getline(cin, s);
	cout << endl;
	getline(cin, s0);
	cout << endl;
	while (s.find(s0) >= 0 && s.find(s0) < s.length() + 1)
	{
		s.erase(s.find(s0), s0.length());
		k++;
	}
	cout << k;
}
